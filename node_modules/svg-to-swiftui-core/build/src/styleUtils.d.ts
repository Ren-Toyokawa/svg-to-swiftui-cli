import { Properties } from 'hast';
import { ElementNode } from 'svg-parser';
declare type StyleData = Record<string, string | number>;
/**
 * Extracts
 * @param element Element node which
 * @param options
 */
export declare function extractStyle(element: ElementNode): StyleData;
/**
 * Converts style property value into a map where key is
 * the style rule and value is the value of that rule.
 * @param style Style property string.
 */
export declare function parseStyle(style: string): StyleData;
/**
 * Filters out just the properties that are considered
 * style properties, i.e. `fill`, `color`, etc.
 * @param props Any properties from the HAST node.
 */
export declare function filterStyleProps(props: Properties): StyleData;
export declare const StylePropertiesSet: Set<string>;
export {};
