import { clampNormalisedSizeProduct, stringifyRectValues } from '../../utils';
export var generateMoveToSwift = function (data, options) {
    var xy = stringifyRectValues({
        x: data.x / options.viewBox.width,
        y: data.y / options.viewBox.height,
    }, options.precision);
    var new_x = clampNormalisedSizeProduct(xy.x, 'width');
    var new_y = clampNormalisedSizeProduct(xy.y, 'height');
    return ["path.move(to: CGPoint(x: " + new_x + ", y: " + new_y + "))"];
};
//# sourceMappingURL=moveToGenerator.js.map